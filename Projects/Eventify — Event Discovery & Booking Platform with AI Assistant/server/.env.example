# Copy to .env and fill in real values. Never commit .env.

MONGO_URI=mongodb://127.0.0.1:27017/eventify
PORT=5000
NODE_ENV=development

# Rate limiting (relaxed automatically when NODE_ENV is not production)
# RATE_LIMIT_DISABLED=true
# RATE_LIMIT_API_MAX=2000
# RATE_LIMIT_AUTH_MAX=200

JWT_SECRET=replace_with_long_random_string
JWT_EXPIRE=7d

CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Stripe (checkout + webhook)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Groq (RAG chatbot — OpenAI-compatible API)
GROQ_API_KEY=
# GROQ_MODEL=llama-3.1-8b-instant

# Optional: npm run seed:admin (min 8 characters)
# SEED_ADMIN_PASSWORD=
# SEED_ADMIN_EMAIL=admin@eventify.local
# SEED_ADMIN_NAME=Eventify Admin
