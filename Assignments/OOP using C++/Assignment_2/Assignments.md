# 🧾 C++ Assignments — OOP Concepts

---

### **Assignment 1: Stack Implementation**

**Objective:**  
Implement a stack using a class with the following:
- Dynamic array for storage
- Methods: `push`, `pop`, `isEmpty`, `getTop`, `showElements`
- Support for **method chaining**
- Use of **static counter** to track created objects
- Destructor for memory deallocation

---

### **Assignment 2: Complex Class**

**Objective:**  
Implement a `Complex` class with:
- Data members: `real`, `img`, and a static counter
- Overloaded constructors (default, one parameter, two parameters)
- Destructor to show object destruction
- Method to print complex numbers in proper format
- Static function to show total active objects

---

### **Assignment 3: BankAccount Class**

**Objective:**  
Design a `BankAccount` class with:
- Data members: `accountHolder`, `accountNumber`, `balance`, static `counter`, and `nextID`
- Auto-generated account numbers
- Constructors (default & parameterized)
- Methods:
  - `deposit()` → supports chaining
  - `withdraw()`
  - `showAccount()`
  - `getBalance()`
  - `showCounter()`